@layout('layouts/master')

@section('content')
    {{ style('assets/styles/auth/register') }}
    <div>
        <h1>Register</h1>

        <form action="{{ route('auth.register') }}" method="post">
            {{ csrfField() }}

            <p>Username</p>
            <input type="text" name="username" id="username" placeholder="JohnDoe">

            <br><br>

            <p>Email</p>
            <input type="text" name="email" id="email" placeholder="johndoe@example.com">
            {{ elIf('<span>$self</span>', getErrorFor('email'), hasErrorFor('email')) }}

            <br><br>

            <p>Password</p>
            <input type="password" name="password" id="password">
            {{ elIf('<span>$self</span>', getErrorFor('password'), hasErrorFor('password')) }}

            <br><br>

            <p>Password Confirmation</p>
            <input type="password" name="password_confirmation" id="password_confirmation">

            <br><br>

            <input type="submit" value="Submit!">

        </form>

        <br><br>

        <a href="{{ route('auth.loginForm') }}">Already have an account?</a>
    <div>
@endsection